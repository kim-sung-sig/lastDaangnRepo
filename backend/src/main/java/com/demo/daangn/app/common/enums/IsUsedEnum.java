package com.demo.daangn.app.common.enums;

public enum IsUsedEnum {
    ENABLED,    // 사용중       (조회 가능)
    DISABLED,   // 사용안함     (조회 가능)
    DELETED,    // 삭제됨       (조회 불가능)
    LOCKED,     // 사용자 계정관련
}
